%h2 会員情報入力
%hr

= form_for @user, url: step2_signup_index_path, method: :get, html: {class: 'first-main__box', novalidate: "novalidate"} do |f|
  .field
    = f.label :nickname, "ニックネーム" 
    %span "必須"
    %br
    = f.text_field :nickname, placeholder: "例）メルカリ太郎"
    - @user.errors.full_messages_for(:nickname).each do |message|
      %li= message

  .field
    = f.label :email, "メールアドレス" 
    %span "必須"
    %br
    = f.email_field :email, placeholder: "PC・携帯どちらでも可"
    - @user.errors.full_messages_for(:email).each do |message|
      %li= message

  .field
    = f.label :password, "パスワード" 
    %span "必須"
    %br
    = f.password_field :password, placeholder: "７文字以上"
    - @user.errors.full_messages_for(:password).each do |message|
      %li= message

  .field
    = f.label :password_confirmation, "パスワード（確認用）" 
    %span "必須"
    %br
    = f.password_field :password_confirmation, placeholder: "７文字以上"
    - @user.errors.full_messages_for(:password_confirmation).each do |message|
      %li= message

  %h3 本人確認
  %p 安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。

  .field
    = f.label :name, "お名前（全角）" 
    %span "必須"
    %br
    = f.text_field :first_name, placeholder: "例）山田"
    = f.text_field :last_name, placeholder: "例）彩"
    - @user.errors.full_messages_for(:first_name).each do |message|
      %li= message
    - @user.errors.full_messages_for(:last_name).each do |message|
      %li= message

  .field
    = f.label :first_kana_name, "お名前カナ（全角）" 
    %span "必須"
    %br
    = f.text_field :first_kana_name, placeholder: "例）ヤマダ"
    = f.text_field :last_kana_name, placeholder: "例）アヤ"
    - @user.errors.full_messages_for(:first_kana_name).each do |message|
      %li= message
    - @user.errors.full_messages_for(:last_kana_name).each do |message|
      %li= message

  .field
    = f.label :birthday, "生年月日" 
    %span "必須"
    %br
    = raw sprintf(f.date_select(:birthday,
                                use_month_numbers: true,
                                date_separator: '%s',
                                start_year: 1900,
                                end_year: Time.now.year,
                                include_blank: true,
                                order: [:year, :month, :day],), '年', '月') + '日'    
    - @user.errors.full_messages_for(:birthday).each do |message|
      %li= message
  %p ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。
  = recaptcha_tags
  %p
    「次へ進む」のボタンを押すことにより、
    %a{href: ""} 利用規約
    に同意したものとみなします。

  .action
    = f.submit "次へ進む"

  %a{href: ""} 本人情報の登録について
  