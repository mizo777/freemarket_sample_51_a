- if @like_products
  %li
    = link_to product_path(product), class: "item-list" do
      %figure
        = image_tag image_url("#{product.product_images.first.image}"), alt: product.name  
      .message-box
        .message
          = product.name
          %p= number_to_currency(product.price)
      = fa_icon "chevron-right"
- else
  %li.not-found.bold いいね！がありません